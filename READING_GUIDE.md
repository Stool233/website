# 外部文章收藏

入口：导航栏 `reading`、首页 Reading shelf 卡片，以及 `/reading` 页面。

每条收藏展示原文标题、来源域名、可选作者、收藏日期、简短推荐理由和最多三个主题。点击标题在新标签页打开原文。收藏按收藏日期倒序排列，支持搜索标题、作者、来源和推荐理由，也能按主题筛选；筛选状态保存在 URL 中。没有 JavaScript 时仍会显示全部收藏及原文链接。

## 添加收藏

编辑 `src/content/reading.json`。它是一个 JSON 数组，每篇文章一个对象：

```json
[
  {
    "id": "unique-article-slug",
    "title": "原文标题",
    "url": "https://example.com/article",
    "author": "原作者（可省略该字段）",
    "savedAt": "2026-09-08",
    "note": "用一两句话说明为什么收藏、哪个观点值得关注，或它与你的实践有什么联系。",
    "tags": ["agent-engineering"]
  }
]
```

上面仅为格式示例，不是实际收藏；发布前填入真实内容。

- `id` 必须唯一，建议使用稳定的英文短标识。
- `url` 使用原文的 HTTP/HTTPS 地址。
- `savedAt` 是收藏日期，与阅读日期保持一致，格式为 `YYYY-MM-DD`；从阅读归档导入时使用对应的阅读日期。
- `note` 是你的推荐理由，必填，最多 1000 字符；用纯文本即可。
- `tags` 可省略或留空；可用主题见 `src/blog-config.ts`，最多三个。
- 设置 `draft: true` 可暂存未整理好的收藏，页面不会展示它。

暂时没有收藏时，页面会显示空状态，不会自动将博客参考文献当作个人收藏。收藏不会混入原创博客列表或博客 RSS。

当前六篇收藏来自 `Stool233/daily-reading` 中明确选定的文章。标题和作者取自归档元信息，链接仅指向原文。推荐理由依据已有笔记整理；本批 `savedAt` 与用户提供的阅读归档日期保持一致。

## 检查

```sh
npm test
npm run build
```
